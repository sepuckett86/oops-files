describe('index', () => {
  beforeEach(() => {
    // make directory with multiple files
  })
  afterEach(() => {
    // delete directory of files
  })
  it('passes', () => {
    // dummy test
  })
})